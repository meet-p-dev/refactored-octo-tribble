# Agent instructions

This repo is the MoneyTrack **web app**. Its companion iOS app is a separate repo
(`~/Documents/MoneyTracker-iOS`, GitHub `meet-p-dev/MoneyTracker-iOS`).

Before any work, read in this order:
1. `core.md`: what MoneyTrack is (web + iOS) and the hard rules
2. `update.md`: what happened recently
3. `todo.md`: open items
4. `CLAUDE.md`: web architecture, verification and deploy rules

Those files are the single source of truth. Don't copy rules into this file.
